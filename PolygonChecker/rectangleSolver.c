#include <stdio.h>

#include "main.h"
#include "rectangleSolver.h"

void assignRecCorner(int* rectanglePoints, int* orderedCorners) {

	for (int i = 0; i < REC_POINTS * 2; i += 2) { // finding lowest x values

	}

	for (int i = 1; i < REC_POINTS * 2; i += 2) { // finding highest y values

	}
}